secret_message = input('Enter your secret message...')
msg = secret_message.split()
secret_code = ''
for word in msg:
    if word == 'a' or word =='the' or word=='an':
        print(word)
    else:
        mystery_word = ' pa'
        secret_code = word + mystery_word
        print(secret_code)
